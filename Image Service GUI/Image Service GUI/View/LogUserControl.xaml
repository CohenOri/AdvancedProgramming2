<UserControl x:Class="Image_Service_GUI.View.LogUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Image_Service_GUI.View"
             xmlns:local2="clr-namespace:Image_Service_GUI.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <DockPanel Background="Ivory" Margin="0">
        <DockPanel.Resources>
            <local2:MessageTypeToBackgroundConverter x:Key="MessageTypeToBackgroundConverter"/>
        </DockPanel.Resources>
        <Label Content="Logs" FontSize="20" HorizontalAlignment="Center" Foreground="Blue" DockPanel.Dock="Top"/>
        <DataGrid ItemsSource="{Binding LogList}" AutoGenerateColumns="False" Background="LightGoldenrodYellow"  ScrollViewer.CanContentScroll="True" Width="Auto" Height="Auto" Margin="10,0,10,20" SelectionChanged="DataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Width="Auto" IsReadOnly="True" Binding="{Binding MsgType}" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Background" Value="{Binding Path=MsgType, Converter={StaticResource MessageTypeToBackgroundConverter}}"></Setter>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Message" Binding="{Binding Msg}" />
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>



</UserControl>
